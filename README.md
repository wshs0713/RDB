# RDB
### RDB Backend (C)
+ Setup
	+ Create database path: [root_dir]/data/db
	+ Makefile
	```
	make
	```

### Webpage (Node.js)
+ Setup
```
npm install -l
```
+ Modify default ip and port in:
	+ public/search.html
	+ views/result.ejs
	+ Default port: 2888

+ Start
```
node server.js
```
